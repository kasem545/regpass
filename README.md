# Password Policy Filter Script

This is a Bash script that interactively filters passwords from a wordlist file based on customizable password policy rules.

## 🚀 Features

- Interactive prompts with defaults
- Tab-completion for file path input
- Filters based on:
  - Minimum and maximum length
  - Uppercase letter requirement
  - Lowercase letter requirement
  - Digit requirement
  - Special character requirement

## 🛠️ Usage

### 1. Make the script executable:

```bash
chmod +x regpass.sh
